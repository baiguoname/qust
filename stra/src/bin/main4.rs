use burn::backend::ndarray::NdArrayDevice;
use burn::backend::{Autodiff, NdArray};
// use qust::prelude::logging_service;
// use stra::p03::runc;



fn main() {
    // logging_service("/root/qust/notebook/git_test/logs".into(), vec!["al".into()]);
    // let device = NdArrayDevice::Cpu;
    // runc::run::<Autodiff<NdArray>>(device);

    // runc::predict();
}